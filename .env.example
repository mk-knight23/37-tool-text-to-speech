# VoiceFlow — Environment Configuration
# Copy this file to .env and fill in your values
# Note: This is a client-side only app. No backend required.

# ===========================================
# Analytics (Optional)
# ===========================================

# Enable anonymous analytics
VITE_ANALYTICS_ENABLED=false

# ===========================================
# Speech Settings
# ===========================================

# Default speech rate (0.1 - 10)
VITE_DEFAULT_RATE=1

# Default speech pitch (0.2 - 2)
VITE_DEFAULT_PITCH=1

# Default volume (0 - 1)
VITE_DEFAULT_VOLUME=1
